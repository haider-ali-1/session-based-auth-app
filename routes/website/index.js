export const renderRegisterPage = (req, res, next) => {
  res.render('register');
};

export const renderLoginPage = (req, res, next) => {
  res.render('login');
};

export const renderDashboardPage = (req, res, next) => {
  res.render('dashboard');
};
